@model X_StateOnline.Core.ViewModels.ProductListVM
@{
    ViewBag.Title = "Home Page";
}
<h1>Products</h1>
<div class="col-md-2">
    <h4>Filter By Category</h4>
    <div class="list-group">
        @Html.ActionLink("All", "Index", null, new { @class = Request["Category"] == null ? "list-group-item-action active" : "list-group-item list-group-item-action" })
        @foreach (var cat in Model.ProductCategories)
        {
            @Html.ActionLink(cat.Category, "Index", new { Category = cat.Category }, new { @class = Request["Category"] == null ? "list-group-item-action active" : "list-group-item list-group-item-action" })
        }
    </div>
</div>
<div class="col-md-10">
    @foreach(var item in Model.Products)
            {
                <div class="col-md-4" style="height:450px;padding:10px;margin:10px;border:solid thin white;">
                    <div class="col-md-12">
                        <img class="img-responsive" style="height:250px" src="~/Content/ProductImages/@item.Image" />
                    </div>
                    <div class="colmd-12">
                        <strong>@Html.ActionLink(item.Name,"Details", new {id = item.Id})</strong>
                    </div>
                    <div class="col-md-12">
                        <p>@item.Description</p>
                    </div>
                    <div class="col-md-12">
                        <h4>R @item.Price</h4>
                    </div>
                    <div class="col-md-12">
                        <a href="@Url.Action("addToCart", "Cart", new { Id = item.Id})" class="btn btn-success">Add to Cart</a>
                    </div>
                </div>
            }
</div>
